# **SPLIT**

*Source-type Probability with Likelihood Identifaction Tools*

Use results from *template fitting* algorithms to attribute source-type, comparing models. Can either run the fitting algorithm or take a table with fitting parameters as input. Use direct best model comparison, or reduced bayesian probability distribution function metrics to compute type.


